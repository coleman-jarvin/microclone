[project]
name = "microclone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
nn = "microclone.nn:main"

[tool.uv.scripts]
nn = "python -m microclone.nn"
